{% extends 'partials/base.html.twig' %}

{% block body %}
    {{ form_errors(form) }}
    {{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.price) }}
    {{ form_row(form.location) }}
    {{ form_row(form.holdingDate) }}
    {{ form_row(form.description) }}
    {{ form_end(form) }}
{% endblock %}
